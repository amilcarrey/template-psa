'use client'
import { Fragment } from 'react'
import { Toaster } from 'react-hot-toast'

const ClientComponentWrapper = () => {
   return <Fragment><Toaster /></Fragment>
}

export default ClientComponentWrapper
